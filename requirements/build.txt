# Requirements for building wheels
# This does not depend on runtime stuff so we do not
# include base.txt
sphinx==1.6.5
sphinx-intl==0.9.10
sphinx-rtd-theme==0.2.4
sphinx-autobuild==0.7.1
pex==1.2.15
wheel==0.30.0
setuptools==38.2.4          # needed to get pex working
